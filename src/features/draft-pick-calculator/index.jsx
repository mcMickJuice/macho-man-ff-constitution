import React from 'react'
import {render} from 'react-dom'
import DraftPickCalculator from './components/Calculator';

const mount = document.getElementById('mount');

render(<DraftPickCalculator />, mount)